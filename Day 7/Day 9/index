(function (){
    const container = document.createElement( tagname'div');
    document.body.appendChild(container);
    container.style{'background-color'} = 'aqua';
    document.body.style.backgroundColor} = 'green';


    function createCarousel(parent,inglist){
        const cContainer = document.createElement( tagname 'div');
        const ingEl = document.createElement( tagname 'ing');
        ingEl.src = ingList(0);
        parent.appendChild(cContainer);
        const leftButton = document.creatElement( tagName 'button');
        leftButton.innertext = '<';
        const rightButton = docuemnt.createElement( tagName 'button');
        rightButton.innertext = '>';
        cContainer.prepend(leftButton);
        cContainer.appendChild(rightButton);
        let ingIndex = 0;
        leftButton.addEventListener 
        
    }
